---
phase: 01-foundation-observation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/__init__.py
  - lib/cli.py
  - lib/commands.py
  - lib/config.py
  - lib/index.py
  - lib/project.py
  - lib/scrub.py
  - lib/sync.py
  - lib/mcp_server.py
  - lib/extract.py
  - lib/review.py
  - lib/sessions.py
  - lib/trigger.py
  - install.sh
  - prism
  - hooks/capture.sh
  - agents/extractor.md
  - agents/validator.md
  - agents/reviewer.md
  - templates/constitution.md
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05
  - SETUP-06
  - SETUP-07

must_haves:
  truths:
    - "All 13 lib/*.py files exist in the Prism repo with zero occurrences of 'engram'/'ENGRAM'/'Engram' (except in historical comments)"
    - "install.sh creates the complete ~/.prism/ tree and is idempotent"
    - "install.sh hard-fails on missing python3/git and soft-warns on missing claude"
    - "The prism CLI wrapper script delegates to lib/cli.py"
    - "hooks/capture.sh is a thin shell wrapper that pipes stdin to python3 lib/capture.py"
  artifacts:
    - path: "lib/config.py"
      provides: "PRISM_HOME, config management, path helpers"
      contains: "PRISM_HOME"
    - path: "lib/project.py"
      provides: "Project detection via git remote SHA256[:12]"
      contains: "PRISM_PROJECT_ID"
    - path: "lib/scrub.py"
      provides: "Secret scrubbing and truncation"
      contains: "scrub_text"
    - path: "lib/cli.py"
      provides: "CLI command router"
      contains: "prog=\"prism\""
    - path: "install.sh"
      provides: "Installer creating ~/.prism/ tree"
      contains: "PRISM_HOME"
    - path: "prism"
      provides: "CLI entry point"
      contains: "from lib.cli import main"
    - path: "hooks/capture.sh"
      provides: "Thin shell wrapper for capture"
      contains: "capture.py"
  key_links:
    - from: "prism"
      to: "lib/cli.py"
      via: "sys.path + import"
      pattern: "from lib.cli import main"
    - from: "hooks/capture.sh"
      to: "lib/capture.py"
      via: "exec python3"
      pattern: "python3.*capture.py"
    - from: "install.sh"
      to: "~/.prism/"
      via: "mkdir + cp"
      pattern: "PRISM_HOME"
---

<objective>
Copy all Engram source files to Prism with comprehensive engram->prism rename, create the install.sh installer, CLI wrapper, thin capture hook shell wrapper, and supporting files (agents, templates).

Purpose: Establish the complete Prism repo file structure that all subsequent plans build upon. This is the foundation -- every other plan depends on these files existing with correct naming.

Output: 13 Python library files, install.sh, prism CLI wrapper, capture.sh hook, 3 agent prompts, 1 constitution template. All with engram->prism renames complete.
</objective>

<execution_context>
@/Users/gaurav/codes/prism/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaurav/codes/prism/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-observation/01-CONTEXT.md
@.planning/phases/01-foundation-observation/01-RESEARCH.md

<interfaces>
<!-- Key source files to copy FROM (Engram). Executor reads these, renames, writes to Prism. -->

Engram lib files to copy (drop team.py, lens.py):
- /Users/gaurav/codes/engram/lib/__init__.py (empty, 0 lines)
- /Users/gaurav/codes/engram/lib/cli.py (362 lines - command router)
- /Users/gaurav/codes/engram/lib/commands.py (697 lines - init, status, learn, etc.)
- /Users/gaurav/codes/engram/lib/config.py (122 lines - config management)
- /Users/gaurav/codes/engram/lib/index.py (146 lines - engram index CRUD)
- /Users/gaurav/codes/engram/lib/project.py (75 lines - project detection)
- /Users/gaurav/codes/engram/lib/scrub.py (31 lines - secret scrubbing)
- /Users/gaurav/codes/engram/lib/sync.py (411 lines - context file generation)
- /Users/gaurav/codes/engram/lib/mcp_server.py (376 lines - MCP JSON-RPC)
- /Users/gaurav/codes/engram/lib/extract.py (338 lines - extraction pipeline)
- /Users/gaurav/codes/engram/lib/review.py (359 lines - session review)
- /Users/gaurav/codes/engram/lib/sessions.py (400 lines - session analysis)
- /Users/gaurav/codes/engram/lib/trigger.py (77 lines - auto-extraction trigger)

Files to DROP (do NOT copy):
- /Users/gaurav/codes/engram/lib/team.py
- /Users/gaurav/codes/engram/lib/lens.py
- /Users/gaurav/codes/engram/hooks/cursor-capture.sh

Other Engram files to copy:
- /Users/gaurav/codes/engram/install.sh (108 lines)
- /Users/gaurav/codes/engram/hooks/capture.sh (123 lines - will be rewritten)
- /Users/gaurav/codes/engram/agents/extractor.md
- /Users/gaurav/codes/engram/agents/validator.md
- /Users/gaurav/codes/engram/agents/reviewer.md
- /Users/gaurav/codes/engram/templates/constitution.md

Comprehensive rename map (from 01-RESEARCH.md):
  ENGRAM_HOME -> PRISM_HOME
  ENGRAM_PROJECT_ID -> PRISM_PROJECT_ID
  ENGRAM_PROJECT -> PRISM_PROJECT
  ~/.engram/ -> ~/.prism/
  .claude/engrams.md -> .claude/prism.md
  engram (CLI command) -> prism
  engram_search -> prism_search
  engram_get -> prism_get
  engram_relevant -> prism_relevant
  engram_record -> prism_record
  engram (MCP server name) -> prism
  "Engram" in user-facing strings -> "Prism"
  "engram" in help text/errors -> "prism"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy and rename all lib/*.py files from Engram to Prism</name>
  <files>
    lib/__init__.py
    lib/cli.py
    lib/commands.py
    lib/config.py
    lib/index.py
    lib/project.py
    lib/scrub.py
    lib/sync.py
    lib/mcp_server.py
    lib/extract.py
    lib/review.py
    lib/sessions.py
    lib/trigger.py
  </files>
  <read_first>
    /Users/gaurav/codes/engram/lib/__init__.py
    /Users/gaurav/codes/engram/lib/cli.py
    /Users/gaurav/codes/engram/lib/commands.py
    /Users/gaurav/codes/engram/lib/config.py
    /Users/gaurav/codes/engram/lib/index.py
    /Users/gaurav/codes/engram/lib/project.py
    /Users/gaurav/codes/engram/lib/scrub.py
    /Users/gaurav/codes/engram/lib/sync.py
    /Users/gaurav/codes/engram/lib/mcp_server.py
    /Users/gaurav/codes/engram/lib/extract.py
    /Users/gaurav/codes/engram/lib/review.py
    /Users/gaurav/codes/engram/lib/sessions.py
    /Users/gaurav/codes/engram/lib/trigger.py
  </read_first>
  <action>
For each of the 13 Engram lib/*.py files listed above (excluding team.py and lens.py per D-01, D-04):

1. Read the Engram source file.
2. Apply the comprehensive rename map. Every occurrence must be replaced:
   - `ENGRAM_HOME` -> `PRISM_HOME` (variable name, references)
   - `ENGRAM_PROJECT_ID` -> `PRISM_PROJECT_ID` (env var name)
   - `ENGRAM_PROJECT` -> `PRISM_PROJECT` (env var name)
   - `~/.engram` -> `~/.prism` (in strings, comments, paths)
   - `.engram` -> `.prism` (path references in strings)
   - `engram` -> `prism` (in MCP server name, CLI command name, user-facing strings, help text, error messages, function/variable names containing "engram")
   - `Engram` -> `Prism` (in docstrings, comments, user-facing strings)
   - `.claude/engrams.md` -> `.claude/prism.md` (context file path)
   - `engram_search` -> `prism_search`, `engram_get` -> `prism_get`, `engram_relevant` -> `prism_relevant`, `engram_record` -> `prism_record` (MCP tool names)
3. Write the renamed file to the Prism repo at the same relative path (lib/filename.py).

Specific per-file changes beyond global rename:

**lib/config.py:**
- `ENGRAM_HOME = Path(os.environ.get("ENGRAM_HOME", os.path.expanduser("~/.engram")))` becomes `PRISM_HOME = Path(os.environ.get("PRISM_HOME", os.path.expanduser("~/.prism")))`
- Add `"review_interval": 5` and `"review_timeout": 60` to DEFAULT_CONFIG if not present (they are in Engram's config.py)
- Add expanded scrub patterns to DEFAULT_CONFIG per 01-RESEARCH.md: `r"AKIA[0-9A-Z]{16}"`, `r"(?i)[a-z]+://[^:]+:[^@\s]+@"`, `r"-----BEGIN\s+\w+\s+PRIVATE\s+KEY-----"`, `r"eyJ[A-Za-z0-9_-]+\.eyJ[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+"`, `r"gho_[A-Za-z0-9]{36}"`, `r"ghs_[A-Za-z0-9]{36}"`, `r"github_pat_[A-Za-z0-9_]{82}"`
- In DEFAULT_CONFIG block_patterns: change `r"(?i)modify\s+engram\s+system"` to `r"(?i)modify\s+prism\s+system"`
- `init_engram_home` -> `init_prism_home`

**lib/cli.py:**
- `prog="engram"` -> `prog="prism"`
- `description="Engram - learned knowledge for Claude Code and Cursor"` -> `description="Prism - knowledge layer for Claude Code"`
- Remove `import-lens`, `publish-to-lens`, `publish`, `pull`, and `observe` subcommands (these reference team.py/lens.py which are dropped per D-01)
- Remove `--cursor` flag from `sync` subcommand (Cursor support dropped)
- Remove `--global` flag from `init` subcommand (Prism init is project-scoped only per design)
- All user-facing strings: `'engram extract'` -> `'prism extract'`, etc.
- In the route for `analyze-sessions`, update any `~/.engram/` path references to `~/.prism/`

**lib/commands.py:**
- Remove `_setup_cursor_hooks()` function entirely (Cursor dropped)
- Remove `_setup_lens_skills()` function entirely (Lens deferred to Phase 3 per D-04)
- Remove the call to `_setup_cursor_hooks()` in `cmd_init()`
- Remove the call to `_setup_lens_skills()` in `cmd_init()`
- Remove `CURSOR_HOOKS` constant
- In `_setup_claude_code_hooks()`: change settings path from `.claude/settings.json` to `.claude/settings.local.json` per D-05
- In `_setup_mcp_server()`: change settings path from `.claude/settings.json` to `.claude/settings.local.json` per D-05
- Remove the `global_hooks` parameter and logic from `cmd_init()`, `_setup_claude_code_hooks()`, `_setup_mcp_server()` -- Prism init is always project-scoped
- Remove Lens API credential handling from `_setup_mcp_server()` (the `lens_api_url`, `lens_token`, `REGISTRY_API_URL`, `REGISTRY_TOKEN` env vars). The MCP env should only have `PRISM_PROJECT`.
- Remove Lens auto-import logic from `cmd_init()` (the `analysis_dir` / `import_lens_skills` block)
- In `CLAUDE_CODE_HOOKS` constant: add `"async": True` to the PostToolUse hook entry per D-08
- Update the hook command paths: `str(ENGRAM_HOME / "hooks" / "capture.sh")` -> `str(PRISM_HOME / "hooks" / "capture.sh")`
- In `cmd_init()` print output: replace `Engram initialized` -> `Prism initialized`, update summary to be concise per D-06, D-11. Remove Lens-related output lines.
- In `cmd_status()`: change `.claude/engrams.md` -> `.claude/prism.md`, remove Cursor context check entirely
- All `"engram extract"` -> `"prism extract"`, `"engram sync"` -> `"prism sync"`, etc.

**lib/trigger.py:**
- `_find_engram_cli()` -> `_find_prism_cli()`
- `shutil.which("engram")` -> `shutil.which("prism")`
- Repo CLI fallback path: `"engram"` -> `"prism"`

**lib/project.py:**
- `ENGRAM_PROJECT_ID` -> `PRISM_PROJECT_ID` in env var name

**lib/mcp_server.py:**
- `ENGRAM_PROJECT` -> `PRISM_PROJECT` (env var)
- Tool names: `engram_search` -> `prism_search`, `engram_get` -> `prism_get`, `engram_relevant` -> `prism_relevant`, `engram_record` -> `prism_record`
- Server name: `"engram"` -> `"prism"` in initialize response

**lib/sync.py:**
- All output file references: `.claude/engrams.md` -> `.claude/prism.md`
- Remove `sync_cursor()` function entirely (Cursor dropped)
- `sync_claude_code()` -> keep this function name but update internal references

**lib/index.py:**
- Apply atomic write pattern from 01-RESEARCH.md Pattern 3: use `fcntl.flock()` + write to `.tmp` + `os.rename()` + backup to `.bak` for `save_index()`. Import `fcntl`, `os`, `shutil` at top.

**lib/__init__.py:** Keep empty.

**lib/extract.py, lib/review.py, lib/sessions.py:** Apply global rename only. These are copied for completeness but not actively used until Phase 2. They must compile without import errors (no references to dropped team.py/lens.py).

4. After writing all files, run a verification grep:
   `grep -rn "engram\|ENGRAM\|Engram" lib/ --include="*.py"` 
   This must return ZERO matches. If any remain, fix them.

Keep Engram's flat lib/*.py structure per D-02 -- no subdirectories.
  </action>
  <verify>
    <automated>cd /Users/gaurav/codes/prism && python3 -c "import sys; sys.path.insert(0,'.'); from lib.config import PRISM_HOME; print('config OK')" && python3 -c "import sys; sys.path.insert(0,'.'); from lib.project import detect_project_id; print('project OK')" && python3 -c "import sys; sys.path.insert(0,'.'); from lib.scrub import scrub_text; print('scrub OK')" && grep -rn "engram\|ENGRAM\|Engram" lib/ --include="*.py" | grep -v "^Binary" | wc -l | xargs test 0 -eq && echo "RENAME CLEAN"</automated>
  </verify>
  <acceptance_criteria>
    - lib/ directory contains exactly 13 .py files: __init__.py, cli.py, commands.py, config.py, index.py, project.py, scrub.py, sync.py, mcp_server.py, extract.py, review.py, sessions.py, trigger.py
    - `grep -rn "engram\|ENGRAM\|Engram" lib/ --include="*.py"` returns 0 matches
    - lib/config.py contains `PRISM_HOME = Path(os.environ.get("PRISM_HOME", os.path.expanduser("~/.prism")))`
    - lib/config.py DEFAULT_CONFIG contains `"AKIA[0-9A-Z]{16}"` (expanded scrub patterns)
    - lib/config.py DEFAULT_CONFIG block_patterns contains `modify\\s+prism\\s+system`
    - lib/project.py contains `PRISM_PROJECT_ID`
    - lib/cli.py contains `prog="prism"`
    - lib/cli.py does NOT contain `import-lens` or `publish-to-lens` subcommands
    - lib/commands.py does NOT contain `_setup_cursor_hooks` or `CURSOR_HOOKS` or `_setup_lens_skills`
    - lib/commands.py contains `settings.local.json` (not `settings.json` for hook/MCP paths)
    - lib/commands.py CLAUDE_CODE_HOOKS PostToolUse entry contains `"async": True`
    - lib/trigger.py contains `_find_prism_cli` and `shutil.which("prism")`
    - lib/mcp_server.py contains `prism_search`, `prism_get`, `prism_relevant`, `prism_record`
    - lib/index.py contains `fcntl.flock` (atomic writes)
    - lib/sync.py does NOT contain `sync_cursor`
    - `python3 -c "import sys; sys.path.insert(0,'/Users/gaurav/codes/prism'); from lib.config import PRISM_HOME"` exits 0
    - No lib/team.py or lib/lens.py exists in the Prism repo
  </acceptance_criteria>
  <done>All 13 lib/*.py files exist in Prism repo with complete engram->prism rename, zero rename misses, all imports resolve, dropped files excluded, atomic index writes implemented, expanded scrub patterns added, Cursor/Lens references removed, settings.local.json used for hooks/MCP.</done>
</task>

<task type="auto">
  <name>Task 2: Create install.sh, CLI wrapper, capture.sh, agents, and templates</name>
  <files>
    install.sh
    prism
    hooks/capture.sh
    agents/extractor.md
    agents/validator.md
    agents/reviewer.md
    templates/constitution.md
  </files>
  <read_first>
    /Users/gaurav/codes/engram/install.sh
    /Users/gaurav/codes/engram/hooks/capture.sh
    /Users/gaurav/codes/engram/agents/extractor.md
    /Users/gaurav/codes/engram/agents/validator.md
    /Users/gaurav/codes/engram/agents/reviewer.md
    /Users/gaurav/codes/engram/templates/constitution.md
    /Users/gaurav/codes/prism/.planning/phases/01-foundation-observation/01-RESEARCH.md
  </read_first>
  <action>
**1. Create install.sh** (adapt from Engram's install.sh per D-07, SETUP-01 through SETUP-07):

The script must:
- Start with `#!/usr/bin/env bash` and `set -euo pipefail`
- Detect PRISM_REPO as the directory containing install.sh: `PRISM_REPO="$(cd "$(dirname "$0")" && pwd)"`
- Set `PRISM_HOME="${PRISM_HOME:-$HOME/.prism}"` and `BIN_DIR="${BIN_DIR:-$HOME/.local/bin}"`
- **Prerequisite checks (D-07):**
  - `command -v python3 >/dev/null 2>&1 || { echo "ERROR: python3 is required but not found."; exit 1; }`
  - `command -v git >/dev/null 2>&1 || { echo "ERROR: git is required but not found."; exit 1; }`
  - `command -v claude >/dev/null 2>&1 || echo "WARNING: claude CLI not found. Needed for extraction (prism extract) but not for observation capture."`
- Create directory structure (SETUP-01): `mkdir -p "$PRISM_HOME"/{global/engrams,archive,hooks,agents,lib,skills,projects}`
- Create bin dir: `mkdir -p "$BIN_DIR"`
- Copy hooks (overwrite on upgrade, SETUP-05): `cp "$PRISM_REPO/hooks/capture.sh" "$PRISM_HOME/hooks/capture.sh"` then `chmod +x "$PRISM_HOME/hooks/capture.sh"`
- Copy agents (overwrite): `cp "$PRISM_REPO/agents/"*.md "$PRISM_HOME/agents/"`
- Copy lib (overwrite): `cp "$PRISM_REPO/lib/"*.py "$PRISM_HOME/lib/"`
- Copy CLI wrapper (overwrite): `cp "$PRISM_REPO/prism" "$PRISM_HOME/prism"` then `chmod +x "$PRISM_HOME/prism"`
- Copy constitution template only if not exists (SETUP-04): `if [ ! -f "$PRISM_HOME/constitution.md" ]; then cp "$PRISM_REPO/templates/constitution.md" "$PRISM_HOME/constitution.md"; fi`
- Write default config.json if not exists (SETUP-03): Use heredoc matching the DEFAULT_CONFIG from lib/config.py (with review_interval, review_timeout, and expanded scrub/block patterns). Exact JSON:
  ```
  {
    "extract_threshold": 15,
    "review_interval": 5,
    "review_timeout": 60,
    "decay_rate_per_week": 0.02,
    "archive_threshold": 0.2,
    "publish_min_confidence": 0.7,
    "max_context_lines": 100,
    "registry_url": ""
  }
  ```
- Write empty index.json if not exists (SETUP-03): `if [ ! -f "$PRISM_HOME/index.json" ]; then echo '{"engrams": []}' > "$PRISM_HOME/index.json"; fi`
- Create symlink (SETUP-02): `ln -sf "$PRISM_HOME/prism" "$BIN_DIR/prism"`
- Check PATH and warn if $BIN_DIR not in PATH
- Print success summary with next steps: `prism init` in a project
- Do NOT include any Lens skill copying (removed per D-04)
- Do NOT include cursor-capture.sh (removed per D-01)
- For SETUP-07 (dual-path install): The script uses `PRISM_REPO` to find source files. When run from git clone, PRISM_REPO is the repo root. For curl|bash, the files would need to be extracted first -- document this in a comment but the primary path is git clone for now (repo is private).

**2. Create prism CLI wrapper** (at repo root as `prism`):

```python
#!/usr/bin/env python3
"""Prism - knowledge layer for Claude Code."""
import os
import sys

# When installed to ~/.prism/prism, lib/ is in the same directory
# When run from repo, lib/ is also in the same directory
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from lib.cli import main

if __name__ == "__main__":
    main()
```

Make this file executable (`chmod +x`).

**3. Create hooks/capture.sh** (thin shell wrapper per D-08, D-09):

```bash
#!/usr/bin/env bash
# Prism capture hook for Claude Code
# Registered as PreToolUse and PostToolUse hook in .claude/settings.local.json
#
# Receives JSON on stdin from Claude Code.
# Pipes directly to a single Python process (D-08, D-09).
# NEVER blocks Claude Code - exit 0 always.

set -euo pipefail

PRISM_HOME="${PRISM_HOME:-$HOME/.prism}"
PHASE="${1:-pre}"

# Guard: don't capture during extraction
[ -f "$PRISM_HOME/.extracting" ] && exit 0

# Pipe stdin directly to Python - single invocation, no shell variable interpolation
# If Python fails for any reason, exit 0 (never block Claude Code)
python3 "$PRISM_HOME/lib/capture.py" "$PHASE" 2>/dev/null || true

exit 0
```

Note: capture.py does not exist yet -- it will be created in Plan 03. The shell wrapper is valid now; it will just silently fail (exit 0) until capture.py is created.

Make this file executable.

**4. Copy agent prompts** from Engram:
- Read `/Users/gaurav/codes/engram/agents/extractor.md`, replace all `engram`/`Engram` with `prism`/`Prism`, write to `agents/extractor.md`
- Read `/Users/gaurav/codes/engram/agents/validator.md`, same renames, write to `agents/validator.md`
- Read `/Users/gaurav/codes/engram/agents/reviewer.md`, same renames, write to `agents/reviewer.md`

**5. Copy constitution template** from Engram:
- Read `/Users/gaurav/codes/engram/templates/constitution.md`, replace `engram`/`Engram` with `prism`/`Prism`, write to `templates/constitution.md`

**6. Final verification:**
- `grep -rn "engram\|ENGRAM\|Engram" install.sh hooks/ agents/ templates/ prism` must return 0 matches
- `bash -n install.sh` must pass (syntax check)
- `bash -n hooks/capture.sh` must pass
- `python3 -c "import ast; ast.parse(open('prism').read())"` must pass
  </action>
  <verify>
    <automated>cd /Users/gaurav/codes/prism && bash -n install.sh && bash -n hooks/capture.sh && python3 -c "import ast; ast.parse(open('prism').read())" && test -x prism && test -x hooks/capture.sh && grep -rn "engram\|ENGRAM\|Engram" install.sh hooks/ agents/ templates/ prism 2>/dev/null | wc -l | xargs test 0 -eq && echo "ALL SHELL+SUPPORT FILES OK"</automated>
  </verify>
  <acceptance_criteria>
    - install.sh exists with `#!/usr/bin/env bash` shebang and `set -euo pipefail`
    - install.sh contains `command -v python3` and `command -v git` checks that exit 1 on failure (D-07)
    - install.sh contains `command -v claude` check that only echoes a WARNING (D-07)
    - install.sh contains `mkdir -p "$PRISM_HOME"/{global/engrams,archive,hooks,agents,lib,skills,projects}` (SETUP-01)
    - install.sh contains `ln -sf "$PRISM_HOME/prism" "$BIN_DIR/prism"` (SETUP-02)
    - install.sh contains heredoc for config.json with `if [ ! -f` guard (SETUP-03)
    - install.sh contains `if [ ! -f "$PRISM_HOME/constitution.md" ]` guard (SETUP-04)
    - install.sh does NOT contain `cursor-capture` or `lens-skills` or `LENS_REPO`
    - `bash -n install.sh` exits 0 (valid syntax)
    - prism file exists, is executable, contains `from lib.cli import main`
    - hooks/capture.sh exists, is executable, contains `python3 "$PRISM_HOME/lib/capture.py" "$PHASE"`
    - hooks/capture.sh does NOT contain `python3 -c` (no inline Python per D-08)
    - agents/extractor.md, agents/validator.md, agents/reviewer.md all exist
    - templates/constitution.md exists
    - `grep -rn "engram\|ENGRAM\|Engram" install.sh hooks/ agents/ templates/ prism` returns 0 matches
  </acceptance_criteria>
  <done>install.sh creates the complete ~/.prism/ tree with prerequisite checks (hard-fail python3/git, soft-warn claude), is idempotent (preserves config/constitution/index), and all supporting files (CLI wrapper, capture hook shell, agents, templates) exist with complete renames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engram source -> Prism repo | Copying code from a trusted source; rename completeness is the risk |
| install.sh -> filesystem | Creates directories and copies files to ~/.prism/ and ~/.local/bin/ |
| capture.sh stdin -> Python | Untrusted JSON from Claude Code hook system piped to Python |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | install.sh | mitigate | install.sh uses `set -euo pipefail` to fail-fast on any error; copies only from known PRISM_REPO directory |
| T-01-02 | I (Information Disclosure) | config.py DEFAULT_CONFIG | mitigate | scrub_patterns include expanded secret patterns (AWS, JWT, connection strings, private keys) per RESEARCH.md |
| T-01-03 | T (Tampering) | index.json writes | mitigate | index.py uses fcntl.flock() + write-to-tmp + os.rename() for atomic writes with .bak backup |
| T-01-04 | S (Spoofing) | capture.sh stdin | mitigate | capture.sh pipes raw stdin to Python (D-09); no shell variable interpolation of untrusted data eliminates shell injection |
| T-01-05 | D (Denial of Service) | capture.sh | mitigate | capture.sh always exits 0; Python failures caught with `|| true`; never blocks Claude Code (D-08) |
| T-01-06 | I (Information Disclosure) | engram->prism rename | mitigate | Post-rename grep verification: `grep -rn "engram\|ENGRAM\|Engram"` must return 0 matches across all files |
</threat_model>

<verification>
1. All 13 lib/*.py files exist and import without errors
2. `grep -rn "engram\|ENGRAM\|Engram" lib/ hooks/ agents/ templates/ install.sh prism --include="*.py" --include="*.sh" --include="*.md"` returns 0 matches
3. `bash -n install.sh && bash -n hooks/capture.sh` both pass
4. `python3 -c "import sys; sys.path.insert(0,'.'); from lib.cli import main; print('CLI OK')"` succeeds
5. install.sh contains prerequisite checks for python3 (hard-fail), git (hard-fail), claude (soft-warn)
</verification>

<success_criteria>
- Complete Prism repo structure exists with all source files
- Zero "engram"/"ENGRAM"/"Engram" references remain in any source file
- install.sh is syntactically valid and contains all SETUP-01 through SETUP-07 requirements
- All Python files import without errors
- No team.py, lens.py, or cursor-capture.sh exists in Prism
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-observation/01-01-SUMMARY.md`
</output>
